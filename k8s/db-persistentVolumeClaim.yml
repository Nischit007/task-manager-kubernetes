apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvcsss
  namespace: default
  labels:
    app: workshop
    component: db
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
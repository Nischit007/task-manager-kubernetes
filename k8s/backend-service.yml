apiVersion: v1
kind: Service
metadata:
  name: task-manager-service
  labels:
    app: workshop
    component: backend
spec:
  type: NodePort
  selector:
    app: workshop
    component: backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
      nodePort: 32000
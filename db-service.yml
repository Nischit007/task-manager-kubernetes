apiVersion: v1
kind: Service
metadata:
  name: task-manager-db-service
  labels:
    app: workshop
    component: db
spec:
  type: ClusterIP
  selector:
    app: workshop
    component: db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
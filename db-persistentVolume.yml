apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  namespace: myapp
  labels:
    app: workshop
    component: db
spec:
  storageClassName:  local-path
  capacity:
    storage:  1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/postgres